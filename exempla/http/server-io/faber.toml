[package]
name = "http-server-io"
version = "0.1.0"
edition = "2026"

[paths]
source = "src"
entry = "main.fab"

[build]
target = "rust"
kind = "bin"

[target.rust]
host = "native"

[locale]
locale = "en"
