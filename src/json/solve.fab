# json/solve.fab — JSON parse facade
#
# Direct `norma:json/solve` facade. The compiler rejects direct
# `textus ↦ json`; the runtime-backed codec belongs behind this Norma API.

functio solve(textus wire) → json ⇥ textus {
    iace "norma:json.solve is deferred pending runtime codec dispatch"
}

functio tempta(textus wire) → json ∪ nihil {
    redde nihil
}
