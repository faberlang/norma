+++
locale = "en"
+++

# pressura.fab - Compression Device
#
# Etymology: "pressura" - pressing, squeezing. Data compression.
#
# Part of the host effect surface (gateway-dispatched `pressura:*` routes).

# Design notes:
# - compress: press data together
# - decompress: loosen/release compressed data
# - Algorithms: gzip, deflate, brotli, zstd
# - Levels: 1 (fast) to 9 (best compression)
# - Sync operations suitable for data <10MB
# - Streaming for large data

# =========================================================================
# ONE-SHOT OPERATIONS
# =========================================================================

# Compress bytes (optional level 1-9)
fn compress(string algorithmus, bytes data, int nivel optional) → bytes {
    panic "norma:pressura.compress is deferred pending Stage 2 dispatch"
}

# Decompress bytes
fn decompress(string algorithmus, bytes data) → bytes {
    panic "norma:pressura.decompress is deferred pending Stage 2 dispatch"
}

# =========================================================================
# STREAMING
# =========================================================================

# Create compression stream (optional level 1-9)
fn create_compressor(string algorithmus, int nivel optional) → Compressor {
    panic "norma:pressura.create_compressor is deferred pending Stage 2 dispatch"
}

# Create decompression stream
fn create_decompressor(string algorithmus) → Decompressor {
    panic "norma:pressura.create_decompressor is deferred pending Stage 2 dispatch"
}

# Compression stream
interface Compressor {
    # Write bytes in
    fn write_bytes(bytes data) → void

    # Close and get compressed result
    fn finish() → bytes
}

# Decompression stream
interface Decompressor {
    # Write compressed bytes in
    fn write_bytes(bytes data) → void

    # Read decompressed bytes out (partial)
    fn read_bytes() → bytes

    # Close and get final result
    fn finish() → bytes
}
